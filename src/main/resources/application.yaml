spring:
  application:
    name: transaction-service

  datasource:
    url: jdbc:postgresql://localhost:5432/buuchezobank_transaction_db #POSTGRESQL DATABASE CONNECTION
    username: postgres
    password: Kadinyah02+++

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8082

#Actuator config, health monitoring
management:
  endpoints:
    web:
      exposure:
        include: "*"

#Eureka
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}